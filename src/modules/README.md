# Application modules

Domain and application modules belong here behind narrow public entry points.

`assessment/` currently contains only the RP-TURN-006 pure TypeScript assessment-domain contract and synthetic local fixtures. It has no React, Next.js route, browser API, persistence or real-user-data dependency. Its scoring output is explicitly provisional and fixture-only.
